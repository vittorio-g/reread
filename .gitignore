# Large simulation results (too big for GitHub)
final_multiverse_results.csv
final_multiverse_results_by_type.csv
final_benchmark_results.csv
mahalanobis_practical_results.csv
multiverse_results.csv
multiverse_results_by_type.csv

# External datasets — include only the 3 validation datasets
external_datasets/*
!external_datasets/README.md
!external_datasets/01_Schroeders_2022/
!external_datasets/06_Schneider_QoL/
!external_datasets/08_Niessen_2016/

# Temporary/diagnostic files (session-specific)
diagnostic_schneider.R
diagnostic_thresholds.R
create_brief.js
_analysis.py

# Generated documents
*.docx

# R workspace
.Rhistory
.RData
.Rproj.user/

# Checkpoints
*_checkpoint.rds

# ---------------------------------------------------------------------------
# Added 2026-08-25: keep the repository to code + small derived artefacts.
# ---------------------------------------------------------------------------

# Third-party benchmark corpora (hundreds of MB, redistributable only under
# their own licences, and re-downloadable from the sources listed in each
# README). The derived scores committed under webapp/ext_bench2/*_y.csv are what
# the analyses actually read.
Dataset/
!Dataset/**/README.md

# Item-level matrices extracted from those corpora: same provenance, same reason.
webapp/ext_bench/*_M.csv
webapp/ext_bench2/*_M.csv
webapp/ext_bench2/*_comp.csv
webapp/ext_bench_oriented/*_M.csv
webapp/sim_bench/

# Build/check output and large intermediates
*.Rcheck/
*.npy
webapp/site/xlsx.full.min.js
_engine_gated.js
*.bak_pre_*

# --- excluded from the public repository (2026-08-25) ---
# First-party study data under embargo until the manuscript is accepted; the
# public release is prepared separately on OSF. The per-respondent labels and
# derived scores stay, because the tool already publishes them in its demo.
Raccolta Dati/
webapp/_study_matrix.csv

# Internal working log: candid process notes, and it names the on-disk location
# of API credentials. Kept locally, not published.
CLAUDE.md
CLAUDE.md.tmp
.claude/scheduled_tasks.lock
