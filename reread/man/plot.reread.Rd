% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.reread}
\alias{plot.reread}
\title{Plot a the reread procedure result}
\usage{
\method{plot}{reread}(x, bins = 40, ...)
}
\arguments{
\item{x}{A \code{"reread"} object.}

\item{bins}{Number of histogram bins (default 40).}

\item{...}{Ignored.}
}
\value{
A \pkg{ggplot2} object (invisibly printed).
}
\description{
Draws the distribution of the ensemble log-odds with the flagging threshold
and the flagged region highlighted. Requires the \pkg{ggplot2} package.
}
\examples{
\donttest{
d <- inject_careless(simulate_clean(12, 6, 300)$data, prevalence = 0.2)
if (requireNamespace("ggplot2", quietly = TRUE)) plot(reread(d$data))
}
}
