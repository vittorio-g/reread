% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{inject_careless}
\alias{inject_careless}
\title{Inject careless responding into clean data}
\usage{
inject_careless(
  data,
  prevalence = 0.2,
  patterns = c("random", "longstring", "pure_straight", "acquiescent", "mixed",
    "fatigue"),
  corruption = c(0.5, 0.6, 0.7, 0.8, 0.9, 1),
  seed = NULL
)
}
\arguments{
\item{data}{A clean numeric matrix or data frame (e.g. from
\code{\link{simulate_clean}}).}

\item{prevalence}{Fraction of respondents to make careless.}

\item{patterns}{Character vector of patterns to draw from (default: all six).}

\item{corruption}{Numeric vector of corruption fractions to draw from (default
0.5 to 1.0).}

\item{seed}{Optional integer seed.}
}
\value{
A list with the corrupted \code{data}, a 0/1 \code{labels} vector, a
  \code{severity} vector (corruption fraction, 0 for clean), and the
  \code{pattern} applied to each careless respondent (\code{NA} for clean).
}
\description{
Corrupts a share of respondents with one of six prototypical careless patterns
-- \code{"random"}, \code{"longstring"}, \code{"pure_straight"},
\code{"acquiescent"}, \code{"mixed"}, and \code{"fatigue"} -- each at a random
corruption fraction. Two of the six (\code{pure_straight}, \code{acquiescent})
are \emph{consistent} patterns that rc is deliberately blind to, so the set is
not stacked in the ensemble's favour.
}
\examples{
clean <- simulate_clean(10, 6, 200, seed = 1)
inj <- inject_careless(clean$data, prevalence = 0.2, seed = 2)
table(inj$labels)
}
